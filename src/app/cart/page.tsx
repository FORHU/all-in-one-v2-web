/**
 * Route entry point only — no business logic here.
 * TODO: resolve the active tenant and render the cart view, composed from
 * `features/storefront` hooks (useCart) and the tenant's presentation layer.
 */
export default function CartPage() {
  return null;
}
