/**
 * Route entry point only — no business logic here.
 * TODO: resolve the active tenant and render the storefront's product listing,
 * composed from `features/storefront` hooks and the tenant's presentation layer.
 */
export default function ProductsPage() {
  return null;
}
