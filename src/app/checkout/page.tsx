/**
 * Route entry point only — no business logic here.
 * TODO: resolve the active tenant and render the checkout flow, composed
 * from `features/storefront` hooks (useCheckout) and the tenant's
 * presentation layer.
 */
export default function CheckoutPage() {
  return null;
}
