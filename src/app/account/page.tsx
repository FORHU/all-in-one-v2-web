/**
 * Route entry point only — no business logic here.
 * TODO: render the authenticated customer's account view, composed from
 * `features/auth` hooks and the tenant's presentation layer.
 */
export default function AccountPage() {
  return null;
}
