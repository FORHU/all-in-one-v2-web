/**
 * Route entry point only — no business logic here.
 * TODO: resolve the active tenant and render the storefront's category
 * listing, composed from `features/storefront` hooks and the tenant's
 * presentation layer.
 */
export default function CategoriesPage() {
  return null;
}
