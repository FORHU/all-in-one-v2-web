/**
 * Domain types for the users feature — re-exported from the Zod contract,
 * which remains the single source of truth (see
 * contracts/users.contract.ts).
 */
export type { User, UsersResponse } from "../contracts/users.contract";
