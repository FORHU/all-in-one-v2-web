/**
 * Domain types for the cms feature — re-exported from the Zod contract,
 * which remains the single source of truth (see contracts/cms.contract.ts).
 */
export type { Page, PageSection } from "../contracts/cms.contract";
