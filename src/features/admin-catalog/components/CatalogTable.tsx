/**
 * Admin Catalog — product/pricing table.
 * TODO: render rows from useCatalog(); this admin surface is shared across
 * tenants (not one of the 5 tenant storefronts), so it lives here directly
 * rather than under tenants/.
 */
export function CatalogTable() {
  return null;
}
