/**
 * Domain types for the admin-catalog feature — re-exported from the Zod
 * contract, which remains the single source of truth (see
 * contracts/catalog.contract.ts).
 */
export type {
  AdminProduct,
  AdminProductsResponse,
} from "../contracts/catalog.contract";
