/**
 * Admin Suppliers — sync status / import table.
 * TODO: render rows from useSuppliers(); shared operator dashboard, not
 * one of the 5 tenant storefronts.
 */
export function SuppliersTable() {
  return null;
}
