/**
 * Domain types for the admin-suppliers feature — re-exported from the Zod
 * contract, which remains the single source of truth (see
 * contracts/suppliers.contract.ts).
 */
export type {
  AdminSupplier,
  AdminSuppliersResponse,
} from "../contracts/suppliers.contract";
