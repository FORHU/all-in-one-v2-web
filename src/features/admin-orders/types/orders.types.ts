/**
 * Domain types for the admin-orders feature — re-exported from the Zod
 * contract, which remains the single source of truth (see
 * contracts/orders.contract.ts).
 */
export type {
  AdminOrder,
  AdminOrdersResponse,
} from "../contracts/orders.contract";
