/**
 * Admin Orders — order/fulfillment status table.
 * TODO: render rows from useOrders(); shared operator dashboard, not one
 * of the 5 tenant storefronts.
 */
export function OrdersTable() {
  return null;
}
