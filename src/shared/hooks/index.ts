/**
 * Shared, cross-feature React hooks that hold no business logic
 * (e.g. useMediaQuery, useDebouncedValue, useLocalStorage).
 * TODO: add hooks here as cross-feature needs emerge.
 */
export {};
